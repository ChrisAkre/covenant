{
  "inputSchema": {
  "$schema": "http://json-schema.org/schema#",
  "type": "object",
  "properties": {
    "data": {
      "type": "object",
      "properties": {
        "clientId": {
          "type": "string"
        },
        "data": {
          "type": "string"
        }
      },
      "required": [
        "clientId",
        "data"
      ]
    }
  },
  "required": [
    "data"
  ]
},
  "spec": {
        "data" : {
            "@&" : "CLIENTNAME"  // verify that we can look down the tree, using refs
        }
    },
  "expectedSchema": {
  "$schema": "http://json-schema.org/schema#",
  "type": "object",
  "properties": {
    "CLIENTNAME": {
      "type": "string"
    }
  },
  "required": [
    "CLIENTNAME"
  ]
}
}